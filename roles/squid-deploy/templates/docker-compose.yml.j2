version: "3"

services:
    squid:
        build:
            context: . 
            dockerfile: Dockerfile
        #image: sameersbn/squid:3.5.27-2
        ports:
            - "3128:3128"
        volumes:
            - ./squid.conf:/etc/squid/squid.conf
        restart: always